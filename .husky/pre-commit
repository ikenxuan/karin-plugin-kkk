if git diff --cached --name-only | grep -qE '^packages/(template|web)/'; then
  echo "🧩 检测到 template/web 改动，更新版本号..."
  pnpm tsx .husky/husky-tasks.ts
  git add packages/core/package.json
fi