if git diff --cached --name-only | grep -qE '^packages/(template)/'; then
  echo "🧩 检测到 template 改动，更新版本号..."
  node --import tsx .husky/husky-tasks.ts
  git add packages/core/package.json
fi